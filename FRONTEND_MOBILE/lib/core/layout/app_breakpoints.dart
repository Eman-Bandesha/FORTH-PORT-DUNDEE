/// Layout width thresholds for responsive UI.
abstract final class AppBreakpoints {
  const AppBreakpoints._();

  /// Minimum width (logical pixels) for tablet / iPad layouts.
  static const double tablet = 768;
}
